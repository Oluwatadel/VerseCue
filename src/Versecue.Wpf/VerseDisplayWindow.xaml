<Window x:Class="Versecue.Wpf.VerseDisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VerseCue Display"
        Width="1100"
        Height="700"
        MinWidth="700"
        MinHeight="450"
        WindowStartupLocation="CenterScreen"
        Background="#111111">

    <Grid Margin="50">

        <ItemsControl x:Name="VerseItemsControl">

            <ItemsControl.ItemTemplate>

                <DataTemplate>

                    <Grid Height="{Binding DisplayHeight}">

                        <Viewbox
                            Stretch="Uniform"
                            StretchDirection="DownOnly"
                            Margin="10,8">

                            <TextBlock
                                Foreground="White"
                                FontSize="42"
                                FontWeight="SemiBold"
                                TextWrapping="Wrap"
                                TextAlignment="Center"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                MaxWidth="900">

                                <Run Text="{Binding Verse.Text}"/>
                                <Run Text=" ("/>
                                <Run Text="{Binding Reference}"/>
                                <Run Text=")"/>

                            </TextBlock>

                        </Viewbox>

                    </Grid>

                </DataTemplate>

            </ItemsControl.ItemTemplate>

        </ItemsControl>

    </Grid>

</Window>
