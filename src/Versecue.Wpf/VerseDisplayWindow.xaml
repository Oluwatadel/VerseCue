<Window x:Class="Versecue.Wpf.VerseDisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VerseCue Display"
        Width="1100"
        Height="700"
        MinWidth="700"
        MinHeight="450"
        WindowStartupLocation="CenterScreen"
        Background="#111111">

    <Grid Margin="50">

        <!--
            The entire verse group scales together.

            This means:
            - 1 short verse = large text
            - 1 long verse = smaller text
            - 2 verses = both scale to fit
            - 3 verses = both/all scale to fit
            - No scrolling
        -->

        <Viewbox
            Stretch="Uniform"
            StretchDirection="DownOnly">

            <ItemsControl
                x:Name="VerseItemsControl"
                Width="950">

                <ItemsControl.ItemTemplate>

                    <DataTemplate>

                        <StackPanel
                            Margin="0,0,0,35">

                            <!-- Verse text -->

                            <TextBlock
                                Text="{Binding Verse.Text}"
                                Foreground="White"
                                FontSize="42"
                                FontWeight="SemiBold"
                                TextWrapping="Wrap"
                                TextAlignment="Center"
                                HorizontalAlignment="Center"
                                MaxWidth="950"/>

                            <!-- Reference -->

                            <TextBlock
                                Text="{Binding Reference}"
                                Foreground="#AAAAAA"
                                FontSize="22"
                                Margin="0,18,0,0"
                                TextAlignment="Center"
                                HorizontalAlignment="Center"/>

                        </StackPanel>

                    </DataTemplate>

                </ItemsControl.ItemTemplate>

            </ItemsControl>

        </Viewbox>

    </Grid>

</Window>