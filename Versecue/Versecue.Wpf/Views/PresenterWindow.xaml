<Window x:Class="Versecue.Wpf.Views.PresenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Scripture Presenter" Height="1080" Width="1920"
        Background="#0F1015" WindowStyle="None" WindowState="Maximized" Topmost="True">
    
    <Grid Margin="100,50,100,100">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Scripture Text -->
        <TextBlock x:Name="TxtScripture"
                   Grid.Row="0"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="#F8FAFC"
                   FontFamily="Cambria, Georgia, serif"
                   FontSize="64"
                   LineHeight="90"
                   Text="Choose a scripture passage and click display to present it here." />

        <!-- Reference -->
        <TextBlock x:Name="TxtReference"
                   Grid.Row="1"
                   TextAlignment="Center"
                   VerticalAlignment="Bottom"
                   Foreground="#A78BFA"
                   FontFamily="Segoe UI, Arial, sans-serif"
                   FontSize="36"
                   FontWeight="SemiBold"
                   Margin="0,40,0,0"
                   Text="Versecue Presenter" />
    </Grid>
</Window>
